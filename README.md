In each project above, it can be started by cloning the repo

add .env file to the root of the project
and add the following to the .env file
MONGODB_URI=the connection string from the mongodb
PORT=the port you want your app to run
JWT_SECRET="enter random strings"
run npm install
then, npm start
